fn appHandler(req: map) {
    io::out("Gelen istek: ", req["path"], " ", req["method"], "\n");
    return {
        "status": 200,
        "body": "{\"message\": \"Proton 6 HTTP Server Calisiyor!\"}"
    };
}

fn main() {
    net::serve(8080, appHandler);
}
